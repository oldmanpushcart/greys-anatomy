<?xml version="1.0" encoding="UTF-8"?>
<mapping>

    <template id="template-desc-expression"><![CDATA[
FOR EXAMPLE
    params[0]
    params[0]+params[1]
    returnObj
    throwExp
    target
    clazz
    method

THE STRUCTURE
          target : the object
           clazz : the object's class
          method : the constructor or method
    params[0..n] : the parameters of method
       returnObj : the returned object of method
        throwExp : the throw exception of method
        isReturn : the method ended by return
         isThrow : the method ended by throwing exception
]]>
    </template>

    <template id="template-desc-condition-expression"><![CDATA[
FOR EXAMPLE
     TRUE : 1==1
     TRUE : true
    FALSE : false
     TRUE : params.length>=0
    FALSE : 1==2

THE STRUCTURE
          target : the object
           clazz : the object's class
          method : the constructor or method
    params[0..n] : the parameters of method
       returnObj : the returned object of method
        throwExp : the throw exception of method
        isReturn : the method ended by return
         isThrow : the method ended by throwing exception
]]>
    </template>


    <template id="template-summary-expression">Path and classname of Pattern Matching</template>
    <template id="template-summary-condition-expression">Method of Pattern Matching</template>

    <template id="template-class-pattern">Path and classname of Pattern Matching</template>
    <template id="template-method-pattern">Method of Pattern Matching</template>
    <template id="template-expand">Expand level of object (0 by default)</template>
    <template id="template-threshold">Threshold of execution times</template>

    <import path="/com/github/ompc/greys/client/res/mapping/info-mapping.xml"/>
    <import path="/com/github/ompc/greys/client/res/mapping/watch-mapping.xml"/>

</mapping>